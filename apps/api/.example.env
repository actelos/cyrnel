# SERVER
PORT=7687
# How long to wait for in-flight requests before forcing shutdown.
SHUTDOWN_TIMEOUT_MS=10000

# LOGGING
# One of: trace | debug | info | warn | error | fatal | silent.
# Defaults to debug in development and info in production.
LOG_LEVEL=info

# AUTH
# If unset, the API runs unauthenticated.
MCI_API_KEY=

# SECRETS
# AES-256-GCM key, base64-encoded, must decode to exactly 32 bytes.
# Generate: openssl rand -base64 32
MCI_SECRETS_KEY=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=

# DATABASE
# If unset, falls back to a local SQLite file in MCI_DATA_DIR.
# file:./mci.db | libsql://<host>?authToken=<token>
MCI_DB_URL=

# STORAGE
# Base dir for SQLite DB and custom modules at <MCI_DATA_DIR>/modules/
MCI_DATA_DIR=./

# REGISTRY
# Allow installing service definitions from private/loopback/link-local
# addresses (e.g. localhost during development). Disabled by default to
# prevent SSRF against internal services. Truthy: 1 | true.
MCI_ALLOW_PRIVATE_REGISTRY=

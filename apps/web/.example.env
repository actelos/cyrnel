# Cyrnel

# API URL + optional key for the Vite dev proxy. Read server-side by the dev
# server (also from the shell environment);.
# CYRNEL_API_URL must stay empty so the client uses same-origin requests
# through the proxy.
CYRNEL_API_URL=http://127.0.0.1:9371
CYRNEL_API_KEY=

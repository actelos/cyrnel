# Cyrnel

# API server URL for dev mode (Vite dev server, no SSR proxy).
# In production, VITE_CYRNEL_API_URL is empty, the SSR server proxies
# same-origin API calls.
VITE_CYRNEL_API_URL=http://127.0.0.1:9371

# Cyrnel

# Browser-side API base URL. Leave empty to use same-origin requests through
# the dev proxy.
VITE_CYRNEL_API_URL=

# API URL for the Vite dev proxy.
CYRNEL_API_URL=http://127.0.0.1:9371

# Optional bearer token injected into dev-server proxy requests.
CYRNEL_API_KEY=

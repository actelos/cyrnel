---
"@cyrnel/mcp": patch
---

Add `missing` filter to `list_modules` and update help text terminology

- **list_modules**: added optional `missing` query parameter to filter by missing state.
- **Tool descriptions**: replaced `orphaned` with `missing` language. Documented `effectivelyEnabled` in `list_services` output.

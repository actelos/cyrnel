---
"@cyrnel/web": patch
---

Rename `orphaned` → `missing` in module pages

- Renamed `orphaned` field to `missing` in Zod schemas, badges, and conditional rendering across ModuleDetailPage, ModulesPage, and ServicesPage.
- Disabled toggle click on missing module cards.

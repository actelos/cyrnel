---
"@cyrnel/sdk": minor
---

Add optional `summary` fields to `ServiceDefinition`, `ToolDefinition`, and `ToolDocsInput` for short plain-text descriptions surfaced in lists, search, and agent-visible tool docs.